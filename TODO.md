# TODO - Ajuste CSS PC

- [x] Corregir desajuste de desktop (PC) en `public/assets/css/style.css` ajustando el layout `.content` para que coincida con el sidebar real `.app-sidebar` (290px), sin tocar el bloque mobile.


